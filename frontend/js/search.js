(()=>{'use strict';const input=document.querySelector('#tool-search'),grid=document.querySelector('#home-tools'),count=document.querySelector('#search-result-count'),pagination=document.querySelector('#search-pagination');if(!input||!grid)return;let tools=[];let page=1;const perPage=12;const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const norm=v=>String(v??'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim();const active=()=>tools.filter(t=>String(t.status||'active').toLowerCase()!=='inactive');const searchable=t=>[t.name,t.description,t.category,t.categoryName,t.slug,t.id,...(Array.isArray(t.tags)?t.tags:[])].map(norm).filter(Boolean);const matches=q=>{const n=norm(q);if(!n)return active();const parts=n.split(/\s+/).filter(Boolean);return active().filter(t=>{const values=searchable(t);return parts.every(part=>values.some(value=>value.includes(part)))})};const icon=t=>{const v=String(t.icon||'').trim();return !v||/^(\/|https?:\/\/|assets\/|\.\.\/|\.\/)/i.test(v)?'🔧':v};const card=t=>`<article class="card tool-card-item"><div class="tool-card-content"><span class="tool-category">${esc(t.categoryName||t.category||'Tool')}</span><div class="tool-card-title-row"><span class="tool-card-icon">${esc(icon(t))}</span><h3>${esc(t.name)}</h3></div><p>${esc(t.description)}</p><a class="card-link" href="${esc(t.url)}" data-tool-id="${esc(t.id)}" data-tool-name="${esc(t.name)}">Open tool →</a></div></article>`;const render=()=>{const found=matches(input.value),total=found.length,totalPages=Math.max(1,Math.ceil(total/perPage));if(page>totalPages)page=totalPages;const start=(page-1)*perPage;grid.innerHTML=found.slice(start,start+perPage).map(card).join('')||'<div class="empty"><strong>No tools found.</strong><br>Try another name, category or tag.</div>';if(count)count.textContent=input.value.trim()?`${total} result${total===1?'':'s'} found`:`${total} active tools`;if(pagination){pagination.innerHTML='';if(totalPages>1){for(let i=1;i<=totalPages;i++){const b=document.createElement('button');b.type='button';b.className='search-page'+(i===page?' active':'');b.textContent=i;b.addEventListener('click',()=>{page=i;render();window.scrollTo({top:0,behavior:'smooth'})});pagination.appendChild(b)}}}};input.addEventListener('input',()=>{page=1;render()});const q=new URLSearchParams(location.search).get('q');if(q){input.value=q}fetch('/data/tools.json?v='+Date.now(),{cache:'no-store'}).then(r=>r.ok?r.json():Promise.reject(new Error('Tool registry unavailable'))).then(d=>{tools=Array.isArray(d?.tools)?d.tools.filter(Boolean):[];render()}).catch(()=>{grid.innerHTML='<div class="empty">Unable to load tools right now. Please refresh the page.</div>'})})();
