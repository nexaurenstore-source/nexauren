(()=>{'use strict';const $=(s,r=document)=>r.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));const render=async()=>{const grid=$('#nx-studios-grid');if(!grid)return;try{const studio=await window.NexaurenRegistry.loadStudios();const tools=await window.NexaurenRegistry.loadTools();const activeTools=new Map(tools.filter(t=>String(t.status||'active')==='active').map(t=>[t.id,t]));const studios=studio.filter(s=>String(s.status||'active')==='active');grid.innerHTML=studios.length?studios.map(s=>{const count=(s.tools||[]).filter(id=>activeTools.has(id)).length;return `<a class="nx-studio-card" href="/studios/${esc(s.slug)}/"><span class="nx-studio-icon">${esc(s.icon||'')}</span><h3>${esc(s.name)}</h3><p>${esc(s.description)}</p><span class="nx-studio-meta">${count} ${count===1?'experience':'experiences'} · Explore →</span></a>`}).join(''):'<div class="nx-empty">Studios are being prepared.</div>'}catch{grid.innerHTML='<div class="nx-empty">Unable to load Studios right now. Please refresh the page.</div>'}};render()})();
