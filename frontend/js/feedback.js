(()=>{'use strict';document.querySelectorAll('[data-mail-form]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const fd=new FormData(form),type=form.dataset.type||'Message',kind=fd.get('problem')||'General',title=fd.get('title')||'',message=fd.get('message')||'';const subject=`Nexauren — ${type}: ${title}`;const body=`Type: ${kind}\n\n${message}\n\nSent from Nexauren.`;location.href=`mailto:nexaurenstore@gmail.com?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`}))})();