(()=>{const $=id=>document.getElementById(id);const fileInput=$('audioFile'),fileName=$('fileName'),fileMeta=$('fileMeta'),processBtn=$('processBtn'),status=$('status'),result=$('result'),resultAudio=$('resultAudio');const isCutter=/\/studios\/audio\/audio-cutter\/?$/.test(location.pathname)||/\/studios\/audio\/cutter\/?$/.test(location.pathname);const STORE='nexauren-audio-cutter-state-v2';let file=null,audioBuffer=null,recorder=null,chunks=[],previewAudio=null,previewUrl=null,resultUrl=null;const openStore=()=>new Promise((resolve,reject)=>{const request=indexedDB.open(STORE,1);request.onupgradeneeded=()=>request.result.createObjectStore('files');request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error)});const saveFile=async f=>{if(!isCutter)return;try{const db=await openStore();db.transaction('files','readwrite').objectStore('files').put(f,'current')}catch{}};const loadFile=async()=>{if(!isCutter)return null;try{const db=await openStore();return await new Promise((resolve,reject)=>{const r=db.transaction('files').objectStore('files').get('current');r.onsuccess=()=>resolve(r.result||null);r.onerror=()=>reject(r.error)})}catch{return null}};const clearSavedFile=async()=>{if(!isCutter)return;try{const db=await openStore();db.transaction('files','readwrite').objectStore('files').delete('current')}catch{}};const formatTime=seconds=>{seconds=Math.max(0,Number(seconds)||0);return `${Math.floor(seconds/60)}:${String(Math.floor(seconds%60)).padStart(2,'0')}`};const updateSelection=()=>{if(!audioBuffer)return;const start=$('start'),end=$('end');if(!start||!end)return;let s=Number(start.value),e=Number(end.value);if(s>e){if(document.activeElement===start)e=s;else s=e;start.value=s;end.value=e}const d=audioBuffer.duration,ss=d*s/100,ee=d*e/100,selection=$('selection');if(selection){selection.style.left=`${s}%`;selection.style.width=`${Math.max(0,e-s)}%`}if($('startOut'))$('startOut').textContent=formatTime(ss);if($('endOut'))$('endOut').textContent=formatTime(ee);if($('startMarker'))$('startMarker').textContent=formatTime(ss);if($('endMarker'))$('endMarker').textContent=formatTime(ee);if($('selectionDuration'))$('selectionDuration').textContent=`Selected ${formatTime(Math.max(0,ee-ss))}`;if($('previewBtn'))$('previewBtn').disabled=!(ee>ss)};const stopPreview=()=>{if(previewAudio){previewAudio.pause();previewAudio.src='';previewAudio=null}if(previewUrl){URL.revokeObjectURL(previewUrl);previewUrl=null}};const previewSelection=()=>{if(!audioBuffer||!$('start')||!$('end'))return;const s=Number($('start').value)/100,e=Number($('end').value)/100;stopPreview();previewUrl=URL.createObjectURL(wavBlob(audioBuffer,audioBuffer.duration*s,audioBuffer.duration*e));previewAudio=new Audio(previewUrl);previewAudio.play().catch(()=>{});if(status)status.textContent='Previewing selection.';previewAudio.onended=()=>{if(status)status.textContent='Ready to process.'}};const decode=async f=>{const AC=window.AudioContext||window.webkitAudioContext;if(!AC)throw new Error('Audio engine unavailable');const ctx=new AC();try{if(ctx.state==='suspended')await ctx.resume();return await ctx.decodeAudioData(await f.arrayBuffer())}finally{ctx.close().catch(()=>{})}};const setFile=async(f,persist=true)=>{if(!f)return;stopPreview();file=f;if(fileName)fileName.textContent=f.name;if(fileMeta)fileMeta.textContent=`${(f.size/1024/1024).toFixed(2)} MB · ${f.type||'audio file'}`;if(persist)saveFile(f);try{audioBuffer=await decode(f);if($('wave'))$('wave').style.display='block';drawWave(audioBuffer);if(processBtn)processBtn.disabled=false;if(status)status.textContent='Ready to process.';updateSelection()}catch{audioBuffer=null;processBtn&&(processBtn.disabled=true);if(status)status.textContent='This audio format is not supported by your browser.'}};$('chooseBtn')?.addEventListener('click',()=>fileInput?.click());fileInput?.addEventListener('change',e=>setFile(e.target.files?.[0]));$('start')?.addEventListener('input',updateSelection);$('end')?.addEventListener('input',updateSelection);$('previewBtn')?.addEventListener('click',previewSelection);const drop=$('drop');['dragenter','dragover'].forEach(e=>drop?.addEventListener(e,x=>{x.preventDefault();drop.classList.add('drag')}));['dragleave','drop'].forEach(e=>drop?.addEventListener(e,x=>{x.preventDefault();drop.classList.remove('drag')}));drop?.addEventListener('drop',e=>setFile(e.dataTransfer.files?.[0]));$('resetBtn')?.addEventListener('click',async()=>{await clearSavedFile();stopPreview();file=null;audioBuffer=null;if(recorder?.state==='recording')recorder.stop();if(fileInput)fileInput.value='';if(fileName)fileName.textContent='No file selected';if(fileMeta)fileMeta.textContent='';if(processBtn)processBtn.disabled=true;if(result)result.classList.remove('show');if(resultAudio)resultAudio.removeAttribute('src');if(resultUrl){URL.revokeObjectURL(resultUrl);resultUrl=null}if($('wave'))$('wave').style.display='none';if(status)status.textContent='Choose an audio file to begin.'});$('recordBtn')?.addEventListener('click',async()=>{if(recorder?.state==='recording'){recorder.stop();$('recordBtn').textContent='Record from microphone';return}try{const stream=await navigator.mediaDevices.getUserMedia({audio:true});chunks=[];recorder=new MediaRecorder(stream);recorder.ondataavailable=e=>e.data?.size&&chunks.push(e.data);recorder.onstop=()=>{stream.getTracks().forEach(t=>t.stop());setFile(new File(chunks,'nexauren-recording.webm',{type:'audio/webm'}))};recorder.start();$('recordBtn').textContent='Stop recording';if(status)status.textContent='Recording…'}catch{if(status)status.textContent='Microphone access was not available.'}});const drawWave=b=>{const c=$('waveCanvas');if(!c)return;const ctx=c.getContext('2d');c.width=Math.max(1,c.clientWidth*2);c.height=Math.max(1,c.clientHeight*2);ctx.clearRect(0,0,c.width,c.height);if(!b)return;const d=b.getChannelData(0),step=Math.max(1,Math.ceil(d.length/c.width));ctx.strokeStyle='#7657ff';ctx.lineWidth=2;ctx.beginPath();for(let x=0;x<c.width;x++){let min=1,max=-1;for(let j=0;j<step;j++){const v=d[x*step+j]||0;min=Math.min(min,v);max=Math.max(max,v)}ctx.moveTo(x,c.height/2+min*c.height*.38);ctx.lineTo(x,c.height/2+max*c.height*.38)}ctx.stroke()};const wavBlob=(buffer,start=0,end=buffer.duration)=>{const rate=buffer.sampleRate,from=Math.max(0,Math.floor(start*rate)),to=Math.min(buffer.length,Math.floor(end*rate)),len=Math.max(0,to-from),ch=buffer.numberOfChannels,buf=new ArrayBuffer(44+len*ch*2),v=new DataView(buf),w=(o,s)=>{for(let i=0;i<s.length;i++)v.setUint8(o+i,s.charCodeAt(i))};w(0,'RIFF');v.setUint32(4,36+len*ch*2,true);w(8,'WAVE');w(12,'fmt ');v.setUint32(16,16,true);v.setUint16(20,1,true);v.setUint16(22,ch,true);v.setUint32(24,rate,true);v.setUint32(28,rate*ch*2,true);v.setUint16(32,ch*2,true);v.setUint16(34,16,true);w(36,'data');v.setUint32(40,len*ch*2,true);let p=44;for(let i=0;i<len;i++)for(let c=0;c<ch;c++){const x=Math.max(-1,Math.min(1,buffer.getChannelData(c)[from+i]||0));v.setInt16(p,x<0?x*32768:x*32767,true);p+=2}return new Blob([buf],{type:'audio/wav'})};processBtn?.addEventListener('click',async()=>{if(window.NexaurenPremiumEditor||/\/studios\/audio\/audio-editor\/?$/.test(location.pathname))return;if(!audioBuffer){if(status)status.textContent='Please choose an audio file first.';return}const processing=$('processing');try{processing?.classList.add('show');processBtn.disabled=true;await new Promise(r=>setTimeout(r,2400));let blob=wavBlob(audioBuffer);if(window.NexaurenAudioTransform)blob=await window.NexaurenAudioTransform(audioBuffer);if(resultUrl)URL.revokeObjectURL(resultUrl);resultUrl=URL.createObjectURL(blob);if(resultAudio)resultAudio.src=resultUrl;const dl=$('downloadBtn');if(dl)dl.onclick=()=>{const a=document.createElement('a');a.href=resultUrl;a.download='nexauren-audio.wav';a.click()};result?.classList.add('show');if(status)status.textContent='Processing complete.'}catch{if(status)status.textContent='Unable to process this audio file.'}finally{processing?.classList.remove('show');processBtn.disabled=false}});window.NexaurenAudioWorkspace={getBuffer:()=>audioBuffer,wavBlob};loadFile().then(saved=>{if(isCutter&&saved&&!file)setFile(saved,false)});})();