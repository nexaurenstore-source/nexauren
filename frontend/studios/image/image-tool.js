(()=>{'use strict';const $=id=>document.getElementById(id),download=(blob,name)=>{const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)};let img=null,file=null,objectUrl=null;const setup=cfg=>{const input=$('file'),status=$('status'),run=$('run'),reset=$('reset'),preview=$('preview'),drop=document.querySelector('.drop');const load=f=>{if(!f||!f.type.startsWith('image/')){status.textContent='Please choose a supported image file.';return}file=f;if(objectUrl)URL.revokeObjectURL(objectUrl);objectUrl=URL.createObjectURL(f);img=new Image();img.onload=()=>{preview.src=objectUrl;preview.hidden=false;status.textContent=`${file.name} · ${img.naturalWidth} × ${img.naturalHeight}px · ${(file.size/1024).toFixed(1)} KB`;if(drop)drop.classList.add('has-file')};img.onerror=()=>{img=null;status.textContent='Unable to read this image.'};img.src=objectUrl};input.onchange=()=>load(input.files[0]);if(drop){['dragenter','dragover'].forEach(e=>drop.addEventListener(e,ev=>{ev.preventDefault();drop.classList.add('dragging')}));['dragleave','drop'].forEach(e=>drop.addEventListener(e,ev=>{ev.preventDefault();drop.classList.remove('dragging')}));drop.addEventListener('drop',ev=>{const f=ev.dataTransfer.files[0];if(f){input.files=ev.dataTransfer.files;load(f)}})}reset.onclick=()=>{input.value='';img=null;file=null;if(objectUrl)URL.revokeObjectURL(objectUrl);objectUrl=null;preview.removeAttribute('src');preview.hidden=true;drop?.classList.remove('has-file');status.textContent='Choose an image to begin.'};run.onclick=async()=>{try{if(!img||!file)throw Error('Choose an image first.');run.disabled=true;status.textContent='Processing locally…';await cfg.run({img,file,download,$,status});}catch(e){status.textContent=e.message||'Unable to process image.'}finally{run.disabled=false}}};const canvas=(w,h)=>{const c=document.createElement('canvas');c.width=Math.max(1,Math.round(w));c.height=Math.max(1,Math.round(h));return c};const render=(c,type,q)=>new Promise((resolve,reject)=>c.toBlob(b=>b?resolve(b):reject(Error('This browser cannot export the selected format.')),type,q));window.NexaurenImage={setup,canvas,render,download,getState:()=>({img,file}),$}})();