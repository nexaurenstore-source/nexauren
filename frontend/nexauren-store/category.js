const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=(v,c='USD')=>Number(v)===0?'Free':new Intl.NumberFormat(undefined,{style:'currency',currency:c}).format(Number(v)||0);
function card(p){return `<article class="product-card"><div class="product-art">${esc(p.icon||'📦')}</div><div class="product-body"><div class="product-meta"><span>${esc(p.tag||p.category||'Digital')}</span><span>★ ${Number(p.rating||0).toFixed(1)}</span></div><h3>${esc(p.name)}</h3><p>${esc(p.shortDescription||p.description||'')}</p><div class="product-foot"><span class="price ${Number(p.price)===0?'free':''}">${money(p.price,p.currency)}</span><div class="mini-actions"><a href="/nexauren-store/product/?slug=${encodeURIComponent(p.slug)}">View</a></div></div></div></article>`}
async function init(){const slug=new URL(location.href).searchParams.get('slug')||'';try{const cr=await fetch('/api/store/categories',{credentials:'include'});const cd=await cr.json();const cat=(cd.categories||[]).find(c=>c.id===slug||c.name.toLowerCase()===slug.toLowerCase());if(!cat)throw new Error('Category not found');document.title=`${cat.name} — Nexauren Store`;document.querySelector('meta[name="description"]').content=cat.description||`Browse ${cat.name} products on Nexauren Store.`;document.getElementById('categoryIcon').textContent=`${cat.icon||'📦'} NEXAUREN · CATEGORY`;document.getElementById('categoryTitle').textContent=cat.name;document.getElementById('categoryDescription').textContent=cat.description||'Browse digital products in this category.';const r=await fetch('/api/store/products?category='+encodeURIComponent(cat.id)+'&limit=48&sort=newest',{credentials:'include'});if(!r.ok)throw new Error('Unable to load products');const d=await r.json();const products=d.products||[];document.getElementById('summary').textContent=`${products.length} published product${products.length===1?'':'s'} in ${cat.name}`;document.getElementById('products').innerHTML=products.map(card).join('');document.getElementById('empty').hidden=products.length>0;}catch(e){document.getElementById('categoryTitle').textContent='Category unavailable';document.getElementById('categoryDescription').textContent=e.message||'Unable to load this category.';document.getElementById('summary').textContent='Unable to load category products.';}}
document.addEventListener('DOMContentLoaded',init);
