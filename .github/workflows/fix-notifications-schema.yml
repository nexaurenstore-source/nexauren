name: Deprecated — Notifications Schema Patch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy notifications schema patching is retired. The Worker build now owns the notification schema module."
