{
  "name": "nexauren",
  "private": true,
  "scripts": {
    "check:worker": "node scripts/build-worker.mjs",
    "check:studios": "node scripts/validate-studios.mjs",
    "check:architecture": "node scripts/validate-studios.mjs",
    "check": "npm run check:studios && npm run check:worker",
    "build": "npm run check:architecture && node scripts/build-worker.mjs",
    "deploy": "npm run check && npx wrangler deploy"
  }
}
